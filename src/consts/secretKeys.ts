export enum AuthCookies {
    REFRESH_TOKEN = 'auth_refresh_token',
    ACCESS_TOKEN = 'auth_access_token',
  }

export enum Mail {
  SMTP_HOST="smtp.gmail.com",
  SMTP_PORT=587,
  SMTP_USER="goshatestauth@gmail.com",
  SMTP_PASSWORD="enotehti123"
}